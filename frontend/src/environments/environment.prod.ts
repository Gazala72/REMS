export const environment = {
    production: true,
    apiUrl: '/api', // Can be replaced automatically by proxy rules or full URL of Render app. E.g., 'https://your-rems-backend-name.onrender.com/api'
    imageUrl: '' // Empty because it's served relatively if everything hosted on same server, or absolute Render app url. 'https://your-rems-backend-name.onrender.com'
};
